BootStrap: docker
From: ubuntu:20.04

%post -c /bin/bash
    set -e
    apt update
    apt install -y git build-essential
    git clone -b demo https://github.com/matekelemen/pterm
    apt remove -y git
    cd pterm
    make
